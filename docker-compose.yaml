version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    networks:
      - couliglig_network

  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-app
    restart: always
    volumes:
      # dnsmasq-managed hosts file (host → container)
      - /etc/dnsmasq.d/robots.hosts:/data/robots.hosts
    environment:
      - HOSTS_FILE=/data/robots.hosts
    ports:
      - "8000:8000"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
    networks:
      - couliglig_network

networks:
  couliglig_network:
    driver: bridge